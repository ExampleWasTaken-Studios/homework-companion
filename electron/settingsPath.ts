const settingsPath = {
  general: {
    autoStart: "settings.general.autoStart",
    hwAcceleration: "settings.general.hardwareAcceleration" 
  },
  customization: {
    perfMode: "settings.customization.performanceMode"
  }
};

export default settingsPath;